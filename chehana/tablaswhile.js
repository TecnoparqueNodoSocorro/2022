function tablaswhile(dato) {
    console.log(`tabla del ${dato}`);
    let acomulador = 1;
    while (acomulador <= veces) {
        console.log(`valor ${dato}*${acomulador} = ${dato * acomulador}`);
        acomulador++
    }

}

